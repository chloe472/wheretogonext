/**
 * Video pipeline: extract frames and analyze each with Gemini vision.
 * Stub until ffmpeg-based frame extraction is wired.
 *
 * @param {string} videoPath - Path to a video file on disk
 */
export async function processVideo(videoPath) {
  void videoPath;
  return {
    framesAnalyzed: 0,
    locations: [],
  };
}
